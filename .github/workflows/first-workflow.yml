name: My First Workflow

on:
  push:
    branches:
      - main
 jobs:
   say-hello:
     runs-on: ubuntu-latest

     steps:
       -name: print Hello
       run: echo "Hello! My github Actions working!"
